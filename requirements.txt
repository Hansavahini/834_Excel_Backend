# Core
Django==5.2.17
# Reads backend/.env. The project has a small built-in fallback reader so it
# still starts without this, but install it: the fallback exists to stop a
# missing dependency silently reverting to development defaults, not to make
# the dependency optional.
python-dotenv==1.1.1
djangorestframework==3.18.0
django-cors-headers==4.9.0

# Excel generation
openpyxl==3.1.5
et_xmlfile==2.0.0

# Django dependencies, pinned so a fresh install reproduces this build
asgiref==3.12.1
sqlparse==0.6.0
tzdata==2026.3

# Uncomment for Postgres. SQLite needs nothing extra.
# psycopg[binary]==3.2.3
